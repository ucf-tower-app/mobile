import RouteView from '../../components/route/RouteView';
import { routeMock } from '../../utils/mocks';

const RouteViewWrapper = () => {
  return <RouteView route={routeMock} />;
};

export default RouteViewWrapper;
