import { View } from 'native-base';
import { ProfileScreenNavigationProp } from '../../utils/types';

const Followers = ({
  navigation,
}: ProfileScreenNavigationProp<'Followers'>) => {
  return <View />;
};

export default Followers;
